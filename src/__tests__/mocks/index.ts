// Mock version of index.ts for testing
import { random_from_list, shuffle, append_randomizer_div } from "../../shared";

// Export all the functions needed by other modules
export { random_from_list, shuffle, append_randomizer_div };
